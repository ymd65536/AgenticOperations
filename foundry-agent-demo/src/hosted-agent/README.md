# Hosted Agent implementation

This directory contains the structured input/output contract used for the NGINX 404 hosted-agent demo.

The repository intentionally keeps the implementation local and deterministic, without exposing arbitrary shell execution or direct SSH access to the agent.
